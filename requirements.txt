Flask==2.1.1
flask_cors==3.1.1
whisper==1.0.0  # Make sure the version is correct based on the installed Whisper version
pathlib==1.0.1  # pathlib is part of Python 3.4+ but may need to be explicitly mentioned if using older versions of Python
